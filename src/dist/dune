;; Generated by mirage.%%VERSION%%

(rule
 (mode (promote (until-clean)))
 (target hello.spt)
;  (enabled_if (= %{context_name} "solo5"))
 (action
  (copy ../hello.spt %{target}))
)

; (install
;  (files hello.spt)
;  (section bin)
;  (package hello-spt))